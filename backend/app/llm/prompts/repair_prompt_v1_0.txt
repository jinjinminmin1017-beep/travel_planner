你上一次输出不符合系统要求。请基于错误原因修复输出。

必须遵守：
1. 只输出 JSON。
2. 必须符合目标 Schema。
3. 不要解释。
4. 不得新增或修改任何车次、航班、价格、时间、余票、数据源。
5. 不得生成候选站、候选机场或新的路线方案；Recommendation 修复只能引用系统给出的 candidate_plan_ids。
6. 如果用户缺少关键出行信息，不要猜测；保留 null 或让输出无法通过校验，由系统向用户追问。
